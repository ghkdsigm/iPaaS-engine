# Orchestrator
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/company_automation?schema=public
PORT=3000
JWT_SECRET=change_me_to_a_long_random_string

# 32 bytes key (base64) for AES-256-GCM
# Example generation: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
PII_VAULT_KEY=replace_with_32byte_base64

# MCP servers
HR_MCP_BASE_URL=http://localhost:4001

# Frontend
NUXT_PUBLIC_API_BASE=http://localhost:8080/api
